// 설정파일, 프로젝트 최상단에 존재
// 설정에 의해 js파일이 컴파일 된다

{
    "include":[
        "src/**/*.ts"
    ],
    "exclude":[
        "node_modules"
    ],
    "compilerOptions":{
        // 컴파일 방식
        "module": "es6",
        // 시작 루트폴더
        "rootDir": "src",
        // 컴파일 후 js파일 생성 폴더
        "outDir": "dist",
        // 컴파일 버전
        "target": "es6",
        // 소스 원본
        "sourceMap": true,
        // 주석 제거
        "removeComments": true
    }
}